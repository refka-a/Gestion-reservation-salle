{% extends 'base.html.twig' %}

{% block title %}Pricing{% endblock %}

{% block body %}
    <h1>Pricing</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pricing.id }}</td>
            </tr>
            <tr>
                <th>Beneficiary</th>
                <td>{{ pricing.idBeneficiary }}</td>
            </tr>
            <tr>
                <th>Salle</th>
                <td>{{ pricing.idSalle }}</td>
            </tr>
            <tr>
                <th>Timing</th>
                <td>{{ pricing.idTiming }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ pricing.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pricing_index') }}">back to list</a>

    <a href="{{ path('pricing_edit', {'id': pricing.id}) }}">edit</a>

    {{ include('pricing/_delete_form.html.twig') }}
{% endblock %}
